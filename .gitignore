.lake/
build/
lake-packages/

.DS_Store
Thumbs.db

*.swp
*.swo
*~
*.bak
*.orig
*.rej

# Claude Code state: worktrees, local settings, skills, commands -- all of it.
# The files already tracked under .claude/ stay tracked (a .gitignore never
# hides a tracked file); untrack them with `git rm -r --cached .claude` if the
# intent is to stop versioning them altogether.
.claude/
.codex/
.opencode/
.gemini/
.cursor/
.aider*
.continue/

# Local reference material: notes and third-party papers, not ours to redistribute.
local/
