# Work — contribute a slice

Load and follow the project skill **`work`**
(`.claude/skills/work/SKILL.md`).

Do not reimplement that ritual here. Optional goal after `/work` is
`$ARGUMENTS` / the rest of the user message.
