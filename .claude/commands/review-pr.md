# Review-pr — adversarial merge gate

Load and follow the project skill **`review-pr`**
(`.claude/skills/review-pr/SKILL.md`).

Do not reimplement that ritual here. Optional target after
`/review-pr` is `$ARGUMENTS` / the rest of the user message (PR
number, URL, or branch).
